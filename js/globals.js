// js/globals.js
// ---------------------------------------------------
// Гарантирует, что все глобали существуют ДО первой отрисовки
// ---------------------------------------------------

// карта и размеры
window.map            = [];       // заполнится generateMap()

// игровые сущности
window.units          = [];       // заполнит newGame()
window.buildings      = [];       // заполнит newGame()

// ход/игрок
window.turn           = 0;
window.currentPlayer  = 1;

// туман войны (инициализируется в rendering.js, но пусть будет)
window.fogMask        = [];

// любые будущие переменные можно добавлять сюда же,
// чтобы не ловить ReferenceError при ранних обращениях.